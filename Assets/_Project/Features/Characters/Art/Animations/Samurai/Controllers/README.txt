This folder is reserved for Samurai Animator Controller assets.

The previous `Samurai.controller` file was only a plain-text placeholder and
caused Unity import errors because it was not a real Animator Controller asset.

Create actual `.controller` assets in the Unity Editor when this folder becomes
part of the live animation pipeline.

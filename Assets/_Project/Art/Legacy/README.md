# Legacy Art

Use this lane for:

- imported packs
- archive backgrounds
- older exploration content
- source material that is useful to keep but is not part of the active production path

Existing `_Project/Art/Backgrounds` and `_Project/Art/References` content should be treated as legacy by default unless explicitly promoted into `Production/`.

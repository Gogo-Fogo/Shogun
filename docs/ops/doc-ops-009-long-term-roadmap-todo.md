# DOC-OPS-009 Long-Term Roadmap TODO

> Purpose: hold real but deferred work so it does not pollute the active vertical-slice backlog.

## Usage rule

If a task does not directly help make `Dev_Sandbox` and `Courtyard Ambush` trustworthy, it belongs here instead of the short-term implementation TODO.

## Content systems

- `[After slice]` Add more authored encounters after `Courtyard Ambush` proves the core battle loop.
- `[Post-production-foundation]` Build a trustworthy reward/progression chain beyond placeholder result handoff.
- `[Post-production-foundation]` Replace the `Barracks` placeholder debug-owned roster with real player ownership/save data and progression hooks.
- `[Post-production-foundation]` Replace the local `Summon` test harness with a backend-authoritative banner, inventory, pity, and odds-disclosure flow.
- `[Post-production-foundation]` Replace the support-scene `MainMenu` front door with a progression-aware home hub once more than the first slice, summon harness, and barracks placeholder need routing.
- `[Launch-scale]` Expand roster breadth, enemy archetypes, and encounter families into a fuller content cadence.
- `[Only if traction]` Build aggressive alt/variant production cadence tied to live-service banners.

## Roguelite mode

- `[After slice]` Prototype the first `Yomi Road` / run-mode slice after the core battle loop is stable.
- `[Post-production-foundation]` Add reusable event-vignette families, shrine events, and relic/blessing pools.
- `[Launch-scale]` Add multiple run biomes, bosses, and seasonal modifiers.
- `[Only if traction]` Add broader roguelite seasonal support and long-tail live-op rotations.

## Co-op / PvP / social

- `[After slice]` Keep friend-support planning alive, but do not implement it before the battle slice is trustworthy.
- `[Post-production-foundation]` Prioritize world bosses and lightweight shared participation before true co-op or PvP.
- `[Launch-scale]` Add async arena, lightweight co-op, and clan wrappers only after the client/server authority model is proven.
- `[Only if traction]` Consider fuller PvP, clan competition, or richer social systems if the game earns the scope.

## Backend / authority model

- `[After slice]` Finalize the authority matrix: what stays local, what becomes server-truth, and what is disallowed offline.
- `[Post-production-foundation]` Add a thin managed backend for account, rewards, and economy-critical flows.
- `[Launch-scale]` Harden auth, reward validation, live config, and event timing with production-hosted services.
- `[Only if traction]` Move to more expensive dedicated multiplayer/session infrastructure only if co-op/PvP truly justifies it.

## Art pipeline maturity

- `[After slice]` Complete the first Gemini -> PixelLab -> runtime asset trial with one trustworthy character package.
- `[Post-production-foundation]` Mature the production lanes for playable sprites, portraits, event vignettes, and source tracking.
- `[Launch-scale]` Expand the style bible, presentation lane, and asset provenance workflow into a true production standard.
- `[Only if traction]` Bring in commissioned artists for flagship units, key art, and premium presentation assets.

## Live-balance and power-creep operations

- `[After slice]` Keep long-term balance rules documented but do not overbuild balance operations before the slice proves fun.
- `[Post-production-foundation]` Add banner review, old-unit refresh rules, and red-flag checks once more units exist.
- `[Launch-scale]` Operationalize live-balance dashboards, refresh cadence, and seasonal tuning loops.
- `[Only if traction]` Invest in full live-balance operations and deeper content analytics once the game sustains a real audience.

## Platform expansion

- `[After slice]` Keep mobile-first assumptions fixed while validating portrait gameplay on phone screens.
- `[Post-production-foundation]` Audit tablet and foldable presentation once the core loop is stable.
- `[Launch-scale]` Add stronger device-tier graphics/performance options across mobile.
- `[Only if traction]` Consider a PC build only after the mobile game proves it deserves the expansion cost.

## Explicitly not now

Do not pull these items into active implementation unless a direct slice blocker appears:

- broad multi-scene rearchitecture
- wide content generation or mass asset migration
- clan-first or PvP-first planning
- launch-scale backend hosting decisions
- platform-expansion work beyond basic mobile-first discipline





